#include "test_util.h"

#include <fstream>

namespace ebbackup {
namespace test {

// Ensures translation unit links when only header helpers are used.
void TestUtilAnchor() {}

}  // namespace test
}  // namespace ebbackup
